# Copy to .env.local for a deployment where the backend lives somewhere
# other than the same host that serves this frontend. Leave unset for local
# dev -- vite.config.ts's dev-server proxy already routes "/api" to the
# backend running on localhost:3000, so this isn't needed there.
VITE_API_URL=https://your-backend-host.example.com/api/v1
