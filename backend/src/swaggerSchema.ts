/**
 * @swagger
 * components:
 *   schemas:
 *     Item:
 *       type: object
 *       properties:
 *         id:
 *           type: string
 *           format: uuid
 *         name:
 *           type: string
 *         sku:
 *           type: string
 *         unit:
 *           type: string
 *         current_stock:
 *           type: number
 *         created_at:
 *           type: string
 *           format: date-time
 */
